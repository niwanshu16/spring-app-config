eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
okta:
  oauth2:
    audience: api://default
    issuer: https://dev-95517027.okta.com/oauth2/default
